// JavaScript expressions

// JavaScript Statement expression

let abc = 'Some expression';

// Working with strings
let stringWithQuotes = 'This is a "string" value';

let banana = "this is a 'banana' "

/*let backticks = 
    <div>
        <h2>Backticks</h2>
    </div>;*/

let id = "123";
let url = 'https://www.example.com/id=${id}user=1';


// Concatenation
// + plus operator

let newUrl = 'https://www.example.com/id=' + id + 'user=1';

/*console.log('Hello, ' +  'World!'); */

console.log(10 + 10);

// Operators

/* Arithmetic Operators 
    '+', '-', '*'. '/', '%', '**' */

    // Sum

let x = 10;
let y = 20;
let additionResult = x + y; // 30

// Substraction

let substractionResult = x -y; // -10

// Multiplication

let multiResult = x * y; // 200

let divisionResult = x / 0; // Infinity

// Power of **
let powerOfTwo = 4 ** 2;

// Module % remainder of a division

let modResult = x % y;

// Comparison 

let compare = x !== y;

let biggerThan = x > y;

let smallerThan = x < y;

let biggerThanOrEqual = x >= 10;

/* Avoid using loose equality */
/* == != */

/* Checks only value */
Boolean(2 == '2'); // true
Boolean(2 === '2') // false

// Strict equality checks for avlue and datatype

// Short Hand Operator

let sum = 0;
sum = sum + x; // 10
sum += y; // 30
sum -= 5; // 20
sum *= 2; // 50 console.log(result);

// This adds 1
sum++; // 51

// This substracts 1
sum--;

let firstName = 'Mario';

firstName = 'Luigi'

firstName = 'Arabaratahan'


console.log(firstName);



/* */