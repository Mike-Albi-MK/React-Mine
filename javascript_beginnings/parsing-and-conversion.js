// PARSING AND CONVERSION

let str = "1234.54";

let parsedNumber = Number(str); // Output: str converted to number


//let parsedNumber = parseInt(str);
//let parsedNumber = parseFloat(str);

//let parsedNumber = +str; //numery; added str to number – str will be coerced to a number

console.log(typeof parsedNumber, parsedNumber); 

